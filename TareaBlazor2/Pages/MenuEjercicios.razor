@page "/menu-ejercicios"

<PageTitle>Menú de Ejercicios</PageTitle>

<h2>Menú de Ejercicios de Lógica</h2>
<p>Selecciona un ejercicio de la lista para comenzar:</p>

<div class="ejercicios-grid">
    @for (int i = 1; i <= 25; i++)
    {
        <div class="ejercicio-card">
            <h3>Ejercicio @i</h3>
            <p>Ejercicio impar @i.</p>
            <a href="@($"/ejercicio/{i}")" class="btn-ejercicio">Ir al Ejercicio @i</a>
        </div>
    }
</div>

<style>
    .ejercicios-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 20px;
        margin-top: 20px;
    }

    .ejercicio-card {
        background-color: #3498db;
        padding: 20px;
        color: white;
        text-align: center;
        border-radius: 8px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

        .ejercicio-card h3 {
            font-size: 1.5em;
            margin-bottom: 10px;
        }

        .ejercicio-card p {
            font-size: 1em;
            margin-bottom: 15px;
        }

    .btn-ejercicio {
        background-color: #2ecc71;
        color: white;
        padding: 10px 15px;
        border: none;
        border-radius: 5px;
        text-decoration: none;
        font-size: 1em;
    }

        .btn-ejercicio:hover {
            background-color: #27ae60;
        }
</style>

